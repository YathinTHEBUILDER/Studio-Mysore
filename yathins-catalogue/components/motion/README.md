# components/motion/

Shared animation wrapper components.
Examples: FadeUpWrapper, StaggerList, ScrollReveal, PageTransitionWrapper.

Wrappers that compose animation.ts variants with Framer Motion's m.*
components. Keeps animation logic out of content components.
